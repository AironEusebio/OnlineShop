spring:
  datasource:
    username: root
    password: MDIAiron123!@#
    url: jdbc:mysql://localhost:3306/shop
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 9191